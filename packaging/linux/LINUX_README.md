# Synapse Linux Package Helpers

These helper files are reference copies for the Linux release package. The actual Linux package is generated by `scripts/build_release_packages.py`.

Quick start inside a Linux package:

```sh
python3 -m pip install -r requirements.txt
./install_tui.sh
./add_synapse_to_path.sh
synapse
```